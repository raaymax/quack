import {assert} from "@std/assert";

Deno.test("GET /api/users/password/:token - password reset token check", async () => {
  assert(false);
});

Deno.test("GET /api/users/password/:token - password reset token check - token expired", async () => {
  assert(false);
});

Deno.test("PUT /api/auth/password/:token - password reset flow", async () => {
  assert(false);
});
